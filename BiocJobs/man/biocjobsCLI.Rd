% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.R
\name{biocjobsCLI}
\alias{biocjobsCLI}
\title{BiocJobs command line interface}
\usage{
biocjobsCLI(args = commandArgs(trailingOnly = TRUE))
}
\arguments{
\item{args}{Command-line arguments; defaults to
\code{commandArgs(trailingOnly = TRUE)}.}
}
\value{
Exits the process when non-interactive; otherwise returns
invisibly.
}
\description{
Entry point for shell-driven use, normally invoked as
\verb{Rscript -e 'BiocJobs::biocjobsCLI()' <command> <pkg> [job] [options]};
\code{biocjobsCLI("help")} prints the command reference.  Run
non-interactively it exits the R session with the command's status, so
the examples below call the underlying functions instead.
}
\examples{
## The CLI exists for shells and build infrastructure; from R, call the
## same functions it dispatches to.
toy <- system.file("examples", "toy", package = "BiocJobs")

jobs <- findJobs(toy)                 # biocjobsCLI(c("list", toy))
names(jobs)

issues <- validateJob(jobs[[1]])      # biocjobsCLI(c("validate", toy))
length(issues)                        # 0 => the CLI would exit 0

## biocjobsCLI(c("nextflow", toy, "toy-normalize", "--out", out))
out <- file.path(tempdir(), "toy_normalize.nf")
nextflowModule(jobs[[1]], file = out)
file.exists(out)
}
