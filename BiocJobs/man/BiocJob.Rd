% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec.R
\name{BiocJob}
\alias{BiocJob}
\title{The BiocJob object}
\value{
A \code{BiocJob} is a named list with the elements above; this page
documents the structure, it is not a function.
}
\description{
A job specification as returned by \code{\link[=readJob]{readJob()}} and
\code{\link[=findJobs]{findJobs()}}. It is a list of class \code{BiocJob}
whose elements are the YAML fields, normalised:
}
\details{
\itemize{
\item \code{name}: job identifier; \code{package}: host package.
\item \code{title}, \code{version} and \code{script}: single strings.
\item \code{tagline}, \code{description} and \code{license}: single strings,
present only when declared.
\item \code{inputs}: entries with \code{name} and \code{format}, plus
\code{label}, \code{help} and \code{required} when declared. An absent
section is an empty list.
\item \code{outputs}: entries with \code{name} and \code{format}, plus
\code{label} and \code{help} when declared.
\item \code{options}: entries with \code{name} and \code{type}, plus
\code{label}, \code{help} and, by type, \code{default}, \code{required},
\code{choices}, \code{min}, \code{max} and \code{allow_chars}.
\item \code{resources}: \code{cpus}, \code{memory_gb} and \code{disk_gb},
when declared.
\item \code{depends}: R packages the script needs beyond the host package.
\item \code{container}: image reference, or \code{NULL} for the default
image.
\item \code{citations}: entries such as \code{doi}.
\item \code{tests}: declared test cases.
\item \verb{_path}: normalised path of the YAML file.
}

\code{format()} and \code{print()} summarise the interface.
}
\examples{
toy <- system.file("examples", "toy", package = "BiocJobs")
job <- findJobs(toy)[[1]]
class(job)
names(job)
job
}
