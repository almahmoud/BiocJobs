#!/bin/bash
# Generated by BiocJobs 0.99.0 for the 'deseq2-differential-expression' job of package DESeq2.
# Run by HTCondor inside the container; do not edit by hand.
set -euo pipefail

Rscript -e 'BiocJobs::execJob("DESeq2", "deseq2-differential-expression")' \
    --counts 'counts.tsv' \
    --coldata 'coldata.tsv' \
    --design '~ condition' \
    --contrast_factor '{{options.contrast_factor}}' \
    --contrast_numerator '{{options.contrast_numerator}}' \
    --contrast_denominator '{{options.contrast_denominator}}' \
    --alpha '0.1' \
    --shrinkage 'apeglm' \
    --prefilter 'true' \
    --prefilter_min_count '10' \
    --prefilter_min_samples '0' \
    --results 'results.tsv' \
    --normalized_counts 'normalized_counts.tsv' \
    --ma_plot 'ma_plot.pdf'

